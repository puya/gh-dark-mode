# Yak package manifest for Rhino Package Manager.
# Keep `version` in sync with `GHDarkModeInfo.Version` in src/GHDarkMode/GHDarkModeInfo.cs.
# After `./scripts/build.sh`, dist/ contains this file, GHDarkMode.gha, and the icon below for `yak build`.
---
name: gh-dark-mode
version: 1.0.0
authors:
  - GH Dark Mode
description: >
  Grasshopper 1 plugin for Rhino 8 (Windows and Mac): toggle the Grasshopper GUI between dark and light
  themes using GH_Skin and grasshopper_gui.xml, with optional per-key color overrides via companion components.
url: "https://github.com/puya/GHDarkMode"
icon: gh-darkmode-main-a.png
keywords:
  - grasshopper
  - dark-mode
  - theme
  - gui
  - skin
  - rhino-8
  - windows
  - mac
